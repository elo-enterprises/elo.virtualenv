---
# defaults file for elo.virtualenv
virtualenv_user: "{{ansible_user}}"
virtualenvwrapper_python: "python3"
virtualenvwrapper_script: "/usr/local/bin/virtualenvwrapper.sh"
virtualenv_home: "~{{virtualenv_user}}/.venvs"
